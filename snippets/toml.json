{
  "pyenv": {
    "body": [
      "[tools]",
      "python = \"${1:latest}\"",
      "",
      "[env]",
      "_.python.venv = { path = \"${2:.venv}\", create = true }"
    ],
    "prefix": "pyenv"
  }
}
